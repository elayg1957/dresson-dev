import ARCamera from "./ARCamera"; // Import ARCamera inside App

function App() {
  return (
    <div>
      <h1>DressOn WebAR</h1>
      <ARCamera /> {/* AR experience is now inside App */}
    </div>
  );
}

export default App;
